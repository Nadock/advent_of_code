name: Test Solutions

on: push

jobs:
  test_solutions:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v3

      - name: Install Dependecies
        run: |
          pip install pipenv
          pipenv install --dev

      - name: Test existing solutions
        run: pipenv run ./aoc.py -t
